import { languages } from '..';
export declare function fileTranslator(objectPath: string, from: languages, to: languages | languages[]): Promise<void>;
export declare function getFileFromPath(objectPath: string): Promise<{
    json_obj: any;
    objectPath: string;
}>;
